import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'granjazul.com.cafe',
  appName: 'La Cafe',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
